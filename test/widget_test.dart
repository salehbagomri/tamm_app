import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without error', (WidgetTester tester) async {
    // سيتم إضافة اختبارات مفصلة لاحقاً
    expect(1, 1);
  });
}
